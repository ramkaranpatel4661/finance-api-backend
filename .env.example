# Database Configuration
# SQLite (default - no installation needed)
DATABASE_URL=sqlite:///./finance_db.sqlite

# PostgreSQL (uncomment if you want to use PostgreSQL)
# DATABASE_URL=postgresql://username:password@localhost:5432/finance_db

# Application Settings
APP_NAME=Finance Data Processing API
APP_VERSION=1.0.0
DEBUG=False

# Server Configuration  
HOST=0.0.0.0
PORT=8000
